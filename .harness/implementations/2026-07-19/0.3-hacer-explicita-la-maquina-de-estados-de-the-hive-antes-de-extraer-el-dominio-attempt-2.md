---
id: 0.3
slug: hacer-explicita-la-maquina-de-estados-de-the-hive-antes-de-extraer-el-dominio
type: implementation
created_at: 2026-07-19T07:18:13Z
created_by_command: /sdd:implement
source_prompt: inferred from thread context
status: completed
plan: .harness/plans/2026-07-18/0.3-hacer-explicita-la-maquina-de-estados-de-the-hive-antes-de-extraer-el-dominio.md
baseline: e3eabf8422181ffc0f17e106f0c50c8364cce59a
---

# Resumen de ejecucion

Se completó la Fase 03 tras resolver el bloqueo local de artefactos `dist` mediante la validación canónica en Docker. La autoridad de fases, locks y acciones se centralizó en una máquina pura de backend; el shell Socket.IO conserva handlers, timers, emisiones y snapshots, mientras el frontend consume capacidades privadas para autorizar cancelar y rechazar una estrella.

# Alcance del cambio

Base: `e3eabf8422181ffc0f17e106f0c50c8364cce59a`

Archivos modificados:

- Añadidos: `apps/backend/src/gameStateMachine.ts`; `apps/backend/src/gameStateMachine.test.ts`; este artefacto.
- Modificados: `.harness/context/architecture.md`; `.harness/context/domain.md`; `AGENTS.md`; el plan fuente (solo `status`); `apps/backend/src/index.ts`; `apps/backend/src/privateState.ts`; `apps/backend/src/privateState.test.ts`; `apps/backend/src/roundParticipants.ts`; `apps/backend/src/roundParticipants.test.ts`; `apps/frontend/src/App.tsx`; `apps/frontend/src/commandActions.ts`; `apps/frontend/src/commandActions.test.ts`; `packages/contracts/src/actions.ts`.
- Renombrados: Ninguno.
- Eliminados: Ninguno.

Cambios preexistentes:

- La implementación bloqueada anterior `.harness/implementations/2026-07-19/0.3-hacer-explicita-la-maquina-de-estados-de-the-hive-antes-de-extraer-el-dominio.md` se conservó sin modificaciones como historial del primer intento.
- El directorio local `apps/frontend/dist/assets` pertenecía a `root`, bloqueando el build local. No se modificó; los builds Docker canónicos produjeron resultados correctos en su entorno aislado.

# Plan ejecutado

- TI-01: completada. Se creó la máquina pura, se modelaron start, ready, dealing, countdown y callbacks obsoletos.
- TI-02: completada. Juego, pausa, error y políticas de participantes consultan la misma evaluación canónica.
- TI-03: completada. Propuesta, aceptación, cancelación, rechazo, confirmación de estrella y locks de settlement están cubiertos por la máquina y capacidades privadas.
- TI-04: completada. Cierre de ronda, nivel, retry y callbacks temporizados reingresan con validación de fase/lock/deadline.
- TI-05: completada. `cancel_star` y `reject_star` se añadieron al contrato y el cliente ya no deriva su habilitación de fase o lock local.
- TI-06: completada. Contextos actualizados con fuente única, fases, políticas, Mermaid y guía de consulta en `AGENTS.md`.

# Cambios realizados

- Se añadió `evaluateGameTransition()` y `commandDecision()` en `gameStateMachine.ts`, con decisiones discriminadas, guardas, patches de fase/lock y rechazo de transiciones temporizadas obsoletas.
- `index.ts` adapta el estado en memoria a la máquina y delega guardas de start, retry, ready, play, pausa y estrella; callbacks temporizados validan su transición antes de mutar el estado autoritativo.
- Las políticas de ready, juego, pausa, consenso y settlement tienen nombres explícitos y pruebas independientes en `roundParticipants.ts`.
- `buildPrivateActions()` consulta la decisión canónica para habilitación y razones. El contrato privado ahora expone `cancel_star` y `reject_star`; `App.tsx` y `commandActions.ts` los consumen sin usar fase/lock local para autorizarlos.
- `domain.md` contiene tabla de fases, invariantes, políticas y diagrama Mermaid `stateDiagram-v2`; `architecture.md` documenta ownership y adaptación técnica.

# Tests y cobertura

- Backend: 62/62 tests correctos; cobertura global 90.66% de líneas; `gameStateMachine.ts` 80.10% de líneas.
- Frontend: 52/52 tests correctos; cobertura global 93.23% de líneas; `commandActions.ts` 80.63% de líneas.
- Contratos: 1/1 tests correcto; cobertura distribuible 100% de líneas.

# Documentacion actualizada

- `.harness/context/domain.md`: fases, invariantes, políticas de elegibilidad y diagrama Mermaid funcional.
- `.harness/context/architecture.md`: máquina pura, adaptación imperativa, timers y fuente única de capacidades.
- `AGENTS.md`: índice actualizado hacia el contexto funcional y técnico de máquina de estados.

# Comandos de validacion ejecutados

- `docker compose run --build --rm --no-deps backend npm test`
- `docker compose run --build --rm --no-deps frontend npm test`
- `docker compose run --build --rm --no-deps backend npm run test:coverage`
- `docker compose run --build --rm --no-deps frontend npm run test:coverage`
- `docker compose run --build --rm --no-deps backend npm run build`
- `docker compose run --build --rm --no-deps frontend npm run build`
- `npm test`, `npm run test:coverage` y `npm run build` en `packages/contracts`
- `git diff --check`

# Bloqueos o desviaciones

- Ninguno.

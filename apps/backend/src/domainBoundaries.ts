/** Backward-compatible API and command alias; layerBoundaries enforces the actual graph. */
export { checkLayerBoundaries as checkDomainBoundaries, inspectLayerSource as inspectDomainSource, type BoundaryIssue } from './tooling/layerBoundaries.js';
